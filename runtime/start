#
#  Resets ecd-feeder and tells it to refill its buffers
#  from host-RAM
#
source api

axi REG_RESET 1
while [ $(get REG_RESET) -eq 1 ]; do
    sleep .1  
done

axi REG_USE_SIM_DATA 1
axi REG_HOST_FRAME_SIZE 0x400000
axi REG_START 1

